# Public Report Output

這個資料夾放公開分享用 HTML 產出檔。

`youtube_radar.py` 會產生：

```text
public\index.html
public\youtube-radar-YYYY-MM-DD.html
```

公開版 HTML 不包含本機 Markdown 路徑，適合上傳到 Google Drive 或未來改接 GitHub Pages。

目前 `public\*.html` 是產出檔，已在 `.gitignore` 中，不提交到 Git。
